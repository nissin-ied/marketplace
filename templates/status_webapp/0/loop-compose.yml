.catalog:
  name: load status private test
  version: 0.0
  uuid: webst:0.0
  questions:
  - variable: RESTART
    description: 'Application restart option: always, on-failure'
    label: 'Restart:'
    required: true
    default: "always"
    type: string
