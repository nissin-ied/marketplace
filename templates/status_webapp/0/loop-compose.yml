.catalog:
  name: load status private test
  version: custom
  uuid: loader-webapp:custom
  questions:
  - variable: RESTART
    description: 'Application restart option: always, on-failure'
    label: 'Restart:'
    required: true
    default: "always"
    type: string
